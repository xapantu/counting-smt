(set-logic QF_ALIA)
(declare-range A (0 10))
(declare-fun a () (Array A Bool))
(declare-fun b () (Array A Bool))
(push 1)
(assert (= a b))
(assert (not (= a b)))
(check-sat)
(pop 1)
(push 1)
(assert (= (select a 0) (select b 0)))
(assert (not (= (select a 0) (select b 0))))
(check-sat)
(pop 1)
(push 1)
(assert (= 10 (# (x A) (= (select a x) (select b x)))))
(assert (not (= 10 (# (x A) (= (select a x) (select b x))))))
(check-sat)
(pop 1)
